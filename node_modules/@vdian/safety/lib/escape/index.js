import { ESCAPE_REGEXP, ESCAPE_MAP } from '../constants';
/**
 * 过滤 < > ' " & ` 字符
 * @param value 字符串
 * @returns 过滤后的字符串
 */
export function escape(value) {
    return value.replace(ESCAPE_REGEXP, function (match) {
        return ESCAPE_MAP[match];
    });
}
