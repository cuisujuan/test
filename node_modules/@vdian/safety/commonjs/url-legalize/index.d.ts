/**
 *
 * @ignore
 */
export declare function isLegalProtocol(protocol: string): boolean;
/**
 * 自定义白名单
 */
export interface LegalUrlOptions {
    /**
     * 追加白名单，默认
     * [
     * '.weidian.com','.koudai.com', '.vdian.com','.geilicdn.com',
     * '.fangxin.com','.ruyu.com','.vdian.net','.91ruyu.com', '.bibikan.cn'
     * ]
     */
    whiteDomains: string[];
    /**
     * 是否追加在默认白名单后面，默认：true
     */
    append?: boolean;
}
/**
 * 检查是否是合法的链接，非法或者空链接返回false
 * @param url  链接
 * @param options  设置白名单
 */
export declare function isLegalUrl(url: string, options?: LegalUrlOptions): boolean;
/**
 * 合法化链接，合法返回绝对路径，不合法返回javascript:;
 * @param url 链接
 * @param options  设置白名单
 * @returns boolean
 */
export declare function legalizeUrl(url: string, options?: LegalUrlOptions): string;
/**
 * 添加域名白名单
 * @param domain 与Cookie Domain设置规则保持一致，例如 .weidian.com 匹配 www.weidian.com,h5.weidian.com，weidian.com 只匹配 weidian.com
 */
export declare function addWhiteDomain(domain: string): void;
