module.exports = () => {
    const home = process.env.HOME;

    if (home.indexOf('/jenkins') !== -1) return 'zebra-not-docker';

    if (home == '/root' || home.indexOf('home/www') !== -1) return 'zebra-docker';

    return 'local';
};