import { Network, NetworkPlugin } from './network';
import { ThorResponse } from './plugins/thor';
import { XMLHttpRequestOptions } from './helper/xhr';
export { getDynamicThor } from './plugins/thor/dynamic-thor';
/**
 * 创建Network实例，可传入默认请求参数，通过该方法可创建多个实例，例如上传实例、Thor实例等。
 * @param options 请求参数，与request options保持一致。
 * @returns Network实例。
 */
export declare function create(options?: XMLHttpRequestOptions): Network;
/**
 * 网络请求。
 * @param url 请求URL。
 * @param options 请求参数，默认为get请求。
 * @returns Promise
 */
export declare function request(url: string, options?: XMLHttpRequestOptions): Promise<ThorResponse>;
/**
 * 注册插件，插件注册后即被立即执行。
 * @param plugin 插件函数，函数作用域为Network实例。
 * @returns Network实例。
 */
export declare function use(plugin: NetworkPlugin): Network;
