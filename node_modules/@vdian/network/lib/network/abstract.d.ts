import { default as Hooks } from '../helper/hooks';
export declare type NetworkPlugin = () => void;
/**
 * @ignore
 */
export declare abstract class AbstractNetwork<AbstractNetworkRequestOptions, AbstractNetworkResponse> {
    options: AbstractNetworkRequestOptions;
    hooks: Hooks;
    private _installedPlugins;
    constructor(options?: AbstractNetworkRequestOptions);
    abstract request(url: string, opts?: AbstractNetworkRequestOptions): Promise<AbstractNetworkResponse>;
    use(plugin: NetworkPlugin): AbstractNetwork<AbstractNetworkRequestOptions, AbstractNetworkResponse>;
}
