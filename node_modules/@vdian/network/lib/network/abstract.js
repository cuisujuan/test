import { default as Hooks } from '../helper/hooks';
import { isFunction } from '../util';
/**
 * @ignore
 */
var AbstractNetwork = /** @class */ (function () {
    function AbstractNetwork(options) {
        this.hooks = new Hooks();
        this._installedPlugins = [];
        this.options = options || {};
        this.hooks = new Hooks();
        this._installedPlugins = [];
    }
    AbstractNetwork.prototype.use = function (plugin) {
        if (this._installedPlugins.indexOf(plugin) > -1 ||
            !isFunction(plugin)) {
            return this;
        }
        plugin.call(this);
        this._installedPlugins.push(plugin);
        return this;
    };
    return AbstractNetwork;
}());
export { AbstractNetwork };
