import { JsbridgeRequestOptions, JsbridgeRequestResponse } from '../../helper/jsbridge';
import { AbstractNetwork } from '../abstract';
/**
 * NativeNetwork类
 * @ignore
 */
export declare class NativeNetwork extends AbstractNetwork<JsbridgeRequestOptions, JsbridgeRequestResponse> {
    constructor(options?: JsbridgeRequestOptions);
    /**
     * 网络请求
     * @param url 请求URL
     * @param opts 请求参数
     * @returns Promise
     */
    request(url: string, opts?: Omit<JsbridgeRequestOptions, 'url'>): Promise<JsbridgeRequestResponse>;
}
