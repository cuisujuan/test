import { AbstractNetwork } from '../abstract';
import { XMLHttpRequestOptions, NetworkRequestResponse } from '../../helper/xhr';
/**
 * Network类
 */
export declare class Network extends AbstractNetwork<XMLHttpRequestOptions, NetworkRequestResponse> {
    constructor(options?: XMLHttpRequestOptions);
    /**
     * 网络请求
     * @param url 请求URL
     * @param opts 请求参数
     * @returns Promise
     */
    request(url: string, opts?: XMLHttpRequestOptions): Promise<NetworkRequestResponse | any>;
}
