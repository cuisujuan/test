import { Network } from '../../network';
export interface ThorResponse {
    status: {
        code: number;
        subcode?: number;
        message?: string;
    };
    result: object | null;
}
/**
 * @ignore
 */
export default function thor(this: Network): void;
