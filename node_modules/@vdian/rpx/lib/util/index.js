"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !exports.hasOwnProperty(p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.replaceRpx = void 0;
var util_1 = require("@vdian/util");
__exportStar(require("./proxy"), exports);
var RPX_REGEXP = /(\.\d+|\d+(\.\d+)?)\s*rpx\b/gi;
/**
 *
 *  替换
 * @ignore
 */
function replaceRpx(value, calculate) {
    if (util_1.isString(value)) {
        value = value.replace(RPX_REGEXP, function (match) {
            // 找出rpx 数字值
            var str = match.substr(0, match.length - 3);
            // string to number
            var number = Number(str);
            return !isNaN(number) ? calculate(number) : match;
        });
    }
    return value;
}
exports.replaceRpx = replaceRpx;
