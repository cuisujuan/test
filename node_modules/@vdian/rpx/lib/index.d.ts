/**
 * rpx 转换成 px
 * @deprecated 使用to方法替代，to返回包含转换后的单位，利于集中控制单位。
 * @param value rpx的值
 * @returns px的值
 */
export declare function rpxToPx(value: number): number;
/**
 * rpx 转换成 rem
 * @deprecated 使用to方法替代，to返回包含转换后的单位，利于集中控制单位。
 * @param value rpx的值
 * @returns rem 的值
 */
export declare function rpxToRem(value: number): number;
/**
 * 将rpx转换为通过目标适配单位(目前是rem)计算后的值。
 * @param value rpx的值
 * @returns 转换后的值和单位
 */
export declare function to(value: number): string;
export interface CaptureOptions {
    resolve?: (value: string) => string;
}
export declare function capture(options?: CaptureOptions): void;
