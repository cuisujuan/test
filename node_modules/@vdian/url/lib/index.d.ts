import { Params } from './querystring';
import { Url } from './url';
/**
 * 参数合并规则
 */
export interface SetQueryStringOptions {
    /**
     * 如果replace=true，则将替换url中相同的参数，避免出现name=1&name=2这类情况。
     * 如果replace=false，则直接将params追加到当前url后。
     */
    replace: true;
}
/**
 * 设置url地址的querystring。
 * @param url 链接地址，如果url为null/undefined，则以当前页面url作为默认值。
 * @param params 参数对象。
 * @param options 设置参数合并规则，具体见参数合并规则。
 * @returns 设置后的url地址。
 */
export declare function setQuerystring(url: string, params: object, options?: SetQueryStringOptions): string;
/**
 * 设置url地址的querystring。
 * @param url 当前页面url。
 * @param params 参数对象。
 * @param options 设置参数合并规则，具体见参数合并规则。
 * @returns 设置后的url地址。
 */
export declare function setQuerystring(params: object, options?: SetQueryStringOptions): string;
/**
 * 获取传入url地址的参数对象。
 * @param url 链接地址 如果url为null/undefined，则以当前页面url作为默认值，如果url为相对地址，不进行url规整化处理。
 * @returns 序列化后的参数对象。
 */
export declare function getQuerystring(url?: string): Params;
export { Url };
